# Console V1 시험 시나리오 — 구현 기준판

이 목록은 Console V1의 현재 범위에 맞춰 코드와 1:1로 대응한다.

> 실제 KIS/OpenDART 네트워크 호출은 아직 연결하지 않았다. 따라서 3번은
> `DEMO 외부 경계 어댑터` 호출 여부를 검사한다. 실제 어댑터 연결 후 같은
> 시험을 KIS/OpenDART 호출 계측으로 교체한다.
>
> Console V1에는 주문 제출 화면이 없다. 따라서 기존 10번의 “주문 제출
> 타임아웃 자동 재전송 금지”는 다음 장으로 이동하고, 현재 단계에서는
> 주문 제출 경로가 명시적으로 차단되는지를 검사한다.

1. 프로그램 시작 시 이전 세션의 `OPEN` 거래 게이트를 재사용하지 않고 `CLOSED`로 시작한다. `HALTED`는 보존한다.
2. 빠른 점검은 `.env`의 KIS 환경변수를 로컬에서 검사하되 외부 경계 어댑터를 호출하지 않는다.
3. 전체 점검은 사용자가 명시적으로 실행할 때만 DEMO 외부 경계 어댑터를 호출하며, 선택 환경의 KIS 자격증명 형식이 잘못되면 `KIS_ENV_INVALID`로 차단한다.
4. 대사 `MISMATCH` 상태에서는 게이트 열기가 거부되고 게이트가 `HALTED`가 된다.
5. 대사 `UNKNOWN` 상태도 정상으로 처리하지 않고 신규 위험을 차단한다.
6. `START TRADING`이 정확히 일치하지 않으면 게이트가 열리지 않는다.
7. 정지 후에는 최신 대사 `MATCH`, 최신 전체 점검 `PASS`, 정상 감사 로그, `RESUME TRADING`, 재가동 사유가 모두 있어야 재가동된다.
8. 거래 정지 명령은 다른 잠금 그룹의 명령이 실행 중이어도 획득·실행할 수 있다.
9. 거래 정지 중에도 계좌 조회·대사·미체결 주문 취소가 작동한다.
10. Console V1의 주문 제출 경로는 `OUT_OF_SCOPE_CONSOLE_V1`로 차단되고 주문이나 자동 재시도가 발생하지 않는다. 주문 타임아웃 복구는 다음 단계에서 구현한다.
11. 저장본 공시 재현은 외부 경계 어댑터를 호출하지 않는다.
12. 같은 저장본을 반복 재현하면 같은 정규화 결과와 `normalized_hash`가 나온다.
13. 같은 `QUERY` 잠금 그룹의 계좌·시세 조회를 연속 실행해도 동시에 시작되지 않는다.
14. 로컬 상태 자동 갱신은 외부 경계 호출 계수를 증가시키지 않는다.
15. JSON 최종 결과와 종료 코드가 모순되면 `CLI_CONTRACT_MISMATCH`로 처리한다.
16. 모든 등록 명령의 argv에 `correlation_id`가 존재한다.
17. 대사에서 시작된 킬 스위치·게이트 정지 이벤트가 같은 `correlation_id`로 추적된다.
18. 감사 로그를 쓸 수 없으면 게이트 열기와 재가동이 차단된다. 거래 정지는 감사 장애 중에도 우선 작동한다.
19. 거래정지 종목은 `price_state=STALE`이고, `display_price`와 보수적 `risk_price`가 분리된다.
20. 재가동 CLI는 GUI의 과거 표시값을 믿지 않고 실행 시점의 권위 상태를 다시 읽어 대사·전체 점검·감사 건강도·UNKNOWN 주문을 재검사한다.

## 자동시험

```bash
python -m pytest -q
```

`tests/test_scenarios.py`의 `test_01_...`부터 `test_20_...`까지가 위 번호와 직접 대응한다.
