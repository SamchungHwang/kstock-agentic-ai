[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kstock-console-v1"
version = "0.9.0"
description = "K-Stock Agentic AI - single user, one fixed account per PAPER/LIVE environment"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
